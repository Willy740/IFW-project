# AI genarated code

# main.py
def parseer_resultaat(uitvoer)


# Test_main.py
in  def test_wrong_order(self, tmpdir)
        |cols = bestand.read().strip().split("\t")
        |assert cols[1] != "CF"
        |assert cols[5] != "rood"
	|
	|AI genarated
